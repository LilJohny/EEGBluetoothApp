<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bluetoothapp">
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activities.ScanActivity"
            android:label="Bluetooth app">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.DeviceActivity" />

        <activity
            android:name=".activities.BackgroundScanActivity"
            android:label="Bluetooth app" />


        <receiver android:name=".receivers.ScanReceiver" />

        <activity
            android:name=".activities.ConnectionActivity"
            android:label="@string/title_example2" />

        <activity
            android:name=".activities.ServiceDiscoveryActivity"
            android:label="@string/title_example3" />
        <activity
            android:name=".activities.PlotActivity"
            android:label="Plot" />

        <activity
            android:name=".activities.CharacteristicOperationActivity"
            android:label="@string/title_example4" />


        <activity
            android:name=".activities.RssiPeriodicActivity"
            android:label="@string/title_example5" />

        <activity
            android:name=".activities.LongWriteActivity"
            android:label="@string/title_example7" />
        <activity android:name=".activities.NotificationPlotActivity"
            android:label="CapLed"/>

    </application>

</manifest>